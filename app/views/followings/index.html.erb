<h1>Followings</h1>

<u>
  <% @followings.each do |following| %>
    <h3 class="center jumbotron">
      <%= link_to(following.username, account_path(following)) %>
      <%= following.email %>
    </h3>
  <% end %>
</u>

<div class="center jumbotron">
  <%= link_to 'Back', account_path(@acc), class: "btn btn-info" %>
</div>
