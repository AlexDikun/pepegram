<!-- <h1>Post</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Message</th>
    </tr>
  </thead>

  <tbody>
    <%@post.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= image_tag post.image_url %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method :delete, data:
                                              { cofirm: "You are sure?"} %></td>
      </tr>
    <% end %>
  </tbody>
-->
